'use strict';

var _interopRequireDefault = require('babel-runtime/helpers/interop-require-default')['default'];

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports['default'] = stripPathPrefix;

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _invariant = require('invariant');

var _invariant2 = _interopRequireDefault(_invariant);

function stripPathPrefix(parent, child) {
  var normalizedParent = _path2['default'].normalize(parent);
  var normalizedChild = _path2['default'].normalize(child);

  (0, _invariant2['default'])(normalizedChild.substr(0, normalizedParent.length) === normalizedParent, 'Linted file is not in parent');

  // also strip the / at the end, which is not in normalizedParent
  return normalizedChild.substr(normalizedParent.length + 1);
}

module.exports = exports['default'];